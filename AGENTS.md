# ImageManager2 项目 Agent 指令

## 默认工作流

处理本项目的开发任务前，阅读并遵循 [子 Agent 协作方案](docs/plans/subagent-workflow.md)。用户在本项目的新对话中只需描述需求，不必重复指定此流程。用户当次明确要求的范围、模型和执行方式优先于本文默认规则。

本项目明确要求：对适合拆分的实施任务，主动使用子 agent 并行工作，无需再次询问是否启用。小改动、紧耦合任务以及单纯问答由主 agent 直接完成；不要为了凑齐角色而派发任务。仅请求分析、解释或方案时，不自动实施代码变更。

## 模型和职责

- 主 agent 默认由 `gpt-6-astra` 担任，负责需求分析、架构、Core 公共契约、关键实现、集成和最终验收。
- 界面和服务实施子 agent 明确使用 `gpt-5.6-terra`，默认最多两个实施者并行；按具体文件分工。
- 中大型或高风险变更在集成后由独立的 `gpt-6-astra` 子 agent 审核，默认只读。
- 禁用 `gpt-5.6-luna`：不得派发、测试连接或用作回退，除非用户明确恢复。
- Terra 不可用时由主 Astra 接手；确定性权限错误不重复重试。接手前确认旧实施者不再写入。
- 不假定可以切换主对话模型；若主模型不是 Astra 或环境不支持指定子模型，如实说明实际执行模型和限制，不伪称已采用所要求的模型。
- 若没有子 agent 工具，由主 agent 按相同阶段完成可执行工作，明确说明未执行并行或独立审核。不要用新建用户可见对话来冒充子 agent。

## 实施规则

1. 先检查项目约定、相关代码和未提交修改，保留用户已有工作。
2. 主 agent 明确预期行为、公共契约、文件所有权和验收方法，再派发实施任务。
3. 每个实施任务必须包含必要上下文、允许修改的文件、接口约定、验证要求和交付格式。同一轮同一文件只交给一个实施者。
4. 界面实施主要负责 App 的指定视图和 ViewModel；服务实施主要负责 Infrastructure 的指定实现。Core 契约、公共注册和共享接线由主 agent 统一协调。
5. 子 agent 遇到契约缺口先报告，不能自行扩大范围或继续转派。主 agent 同时处理不重叠工作，不重复实施已派出的任务。
6. 确认执行环境实际共享或隔离代码的方式；隔离副本须具备正确基线，返回后检查并集成，不能假设未提交修改会自动复制。
7. 在最终集成代码上执行适当构建、针对性测试和必要交互验证。独立审核依据原始需求、契约、实际差异和验证证据；问题修复后复验。
8. 报告已完成行为、实际验证结果和未解决限制。不把未执行检查写成通过；不因默认工作流擅自提交、推送或发布。

## 架构边界

- `ImageManager.App`：Avalonia 桌面 UI 和 MVVM，不直接访问数据库。
- `ImageManager.Core`：领域模型和服务接口，不依赖 App 或 Infrastructure。
- `ImageManager.Infrastructure`：数据库、图像、缓存和推理实现，不依赖 App。
- `ImageManager.Common`：公共工具，避免扩大依赖。

职责以实际代码为准，不为方便分工引入无关重构。数据库迁移、文件删除、复杂并发和 GPU 资源生命周期等高风险部分优先由主 Astra 处理。
